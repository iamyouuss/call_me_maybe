[project]
name = "Call_Me_Maybe"
version = "1.0.0"
description = "Function calling with LLM"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic",
    "numpy",
    "torch",
    "transformers"
]
[tool.uv.sources]
llm-sdk = { path = "./llm_sdk" }